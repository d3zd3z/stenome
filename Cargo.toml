[package]
name = "stenome"
version = "0.1.0"
authors = ["davidb"]

[dependencies]
termion = "1.3"
serde_json = "0.9"
serde = "0.9"
serde_derive = "0.9"
time = "0.1"

[dev-dependencies]
tempdir = "0.3"

# If we optimize the tests, it is reasonable to run the full tests.
#[profile.test]
#opt-level = 3
